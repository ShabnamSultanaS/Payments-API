# Payments-API
Every project I've built ends with data landing in a table or a file. Several roles I'm applying for name API development as a requirement — this is where I learned what it actually takes to expose data as a service instead: resource naming, status codes, response models, and versioning, not just "write a function that returns JSON.
